import React from 'react';
import ReactDOM from 'react-dom/client';
import Popup from '@/entry_points/Popup';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <Popup />
  </React.StrictMode>
);